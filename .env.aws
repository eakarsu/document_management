# Environment Configuration for AWS Deployment
# IMPORTANT: Update all placeholders with your actual AWS values

NODE_ENV=production

# Database Configuration (Docker Compose PostgreSQL)
DATABASE_URL=postgresql://postgres:changeme@postgres:5432/dms_dev
MONGODB_URL=mongodb://dms_user:dms_password@localhost:27017/dms_documents
REDIS_URL=redis://redis:6379

# PostgreSQL Connection Details
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=dms_dev

# Elasticsearch (Optional - disable if not using)
ELASTICSEARCH_URL=http://localhost:9200

# MinIO/S3 Storage Configuration
# Option 1: Use AWS S3 (Recommended for production)
STORAGE_TYPE=s3
S3_BUCKET_NAME=richmond-dms-uploads
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_aws_access_key_id_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key_here

# Option 2: Use MinIO (Alternative to S3)
# STORAGE_TYPE=minio
# MINIO_ENDPOINT=localhost:9000
# MINIO_ACCESS_KEY=dms_user
# MINIO_SECRET_KEY=dms_password
# MINIO_SECURE=false

# Authentication - CHANGE THESE IN PRODUCTION!
JWT_SECRET=dms_super_secret_jwt_key_change_in_production_12345
JWT_REFRESH_SECRET=dms_super_secret_refresh_key_change_in_production_67890

# OpenRouter AI Services
OPENROUTER_API_KEY=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

# AI Model Configuration
OCR_MODEL=anthropic/claude-3-haiku
CLASSIFICATION_MODEL=anthropic/claude-3-haiku
EXTRACTION_MODEL=anthropic/claude-3-sonnet
ANALYSIS_MODEL=anthropic/claude-sonnet-4
SUMMARY_MODEL=openai/gpt-3.5-turbo

# Workflow AI Models
WORKFLOW_AI_MODEL=anthropic/claude-sonnet-4
DECISION_SUPPORT_MODEL=anthropic/claude-sonnet-4
PREDICTION_MODEL=anthropic/claude-sonnet-4
NLP_WORKFLOW_MODEL=anthropic/claude-sonnet-4
COLLABORATION_MODEL=anthropic/claude-sonnet-4
CONFLICT_RESOLUTION_MODEL=anthropic/claude-sonnet-4
ANALYTICS_MODEL=anthropic/claude-sonnet-4

# AI Cache Configuration (in milliseconds)
AI_CACHE_TIMEOUT=30000

# Backend Server Configuration
BACKEND_PORT=4000
# IMPORTANT: Update with your EC2 public IP or domain
BACKEND_URL=http://35.92.161.166:4000

# Frontend Configuration
FRONTEND_PORT=3000
# IMPORTANT: Update with your EC2 public IP or domain
FRONTEND_URL=http://35.92.161.166:3000
NEXT_PUBLIC_API_URL=http://35.92.161.166:4000
NEXT_PUBLIC_COMPANY_NAME=Richmond DMS
NEXT_PUBLIC_COMPANY_LOCATION=Richmond, VA

# CORS Origins (comma-separated) - Update with your domain
ALLOWED_ORIGINS=http://35.92.161.166:3000,http://35.92.161.166:4000

# File Upload Limits
MAX_FILE_SIZE=104857600
MAX_BATCH_SIZE=10

# Rate Limiting
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_PER_HOUR=1000

# Logging
LOG_LEVEL=info

# Development vs Production
DEBUG=false

# Application Mode Configuration
APP_MODE=production
NEXT_PUBLIC_APP_MODE=production
# Authentication Mode
NEXT_PUBLIC_ENABLE_LOGIN=true

# RabbitMQ (if using background processing) - Optional
# RABBITMQ_URL=amqp://dms_user:dms_password@localhost:5672

# Email Configuration (AWS SES Recommended for production)
# Option 1: AWS SES
AWS_SES_REGION=us-east-1
FROM_EMAIL=noreply@yourdomain.com

# Option 2: SMTP (Gmail, etc)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password
# FROM_EMAIL=noreply@yourdomain.com

# Integration URLs (for your existing platforms)
LEGAL_AI_FORMS_URL=https://legalaiforms.com
NORSHIN_VISION_URL=https://norshin.com
CASHFLOW_APP_URL=https://cashflowapp.app
CONTRACTS_URL=https://contracts.orderlybite.com
SECURITY_URL=https://security.orderlybite.com

# Domain and Organization
DEFAULT_ORGANIZATION_DOMAIN=yourdomain.com
COMPANY_NAME=Richmond DMS
COMPANY_LOCATION=Richmond, VA
